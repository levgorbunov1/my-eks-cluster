name: Test Ephemeral Runner
on:
  workflow_dispatch:

jobs:
  test_workflow_1:
    runs-on: [self-hosted, ephemeral-demo]
    steps:
      - run: echo hi there
  test_workflow_2:
    runs-on: [self-hosted, ephemeral-demo]
    steps:
      - run: echo hi there
  test_workflow_3:
    runs-on: [self-hosted, ephemeral-demo]
    steps:
      - run: echo hi there